# Checkpoint

Place pre-trained or fine-tuned model weights in this directory.

Suggested files:

- `base_model.pth`
- `best_checkpoint.pth`

Large weight files are intentionally not bundled in this repository snapshot.
