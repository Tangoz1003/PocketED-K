# Data

This repository does not bundle the raw matched ECG-laboratory cohort.

Use this directory for:

- a matched cohort CSV with WFDB paths and ECG interval metadata
- a temporal split CSV generated by `prepare_split.py`
- lightweight examples or schema files

The `examples/` subdirectory only contains column templates, not patient-level source data.
