# Outputs

This directory stores generated training and inference artifacts.

- `examples/` contains lightweight, non-sensitive reference outputs.
- `train_run/` and `inference_run/` are ignored by default and can be recreated locally.
